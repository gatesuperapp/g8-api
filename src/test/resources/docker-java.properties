# docker-java defaults its initial /info probe to API 1.32, which Docker Engine 27+
# rejects with HTTP 400 ("client version 1.32 is too old"). Pin the API version to a
# value every modern daemon supports (minimum 1.40). Testcontainers 2.x negotiates
# automatically and this file can be removed once we upgrade.
api.version=1.44
